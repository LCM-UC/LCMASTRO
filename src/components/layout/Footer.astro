---
interface Props {
  year?: number;
  author?: string;
  institution?: string;
}

const { 
  year = new Date().getFullYear(),
  author = "Leonardo Canorio",
  institution = "Universidad Continental"
} = Astro.props;
---

<footer class="footer">
  <div class="footer-content">
    <p class="footer-text">
      © {year} {author}
    </p>
    <div class="footer-divider"></div>
    <p class="footer-institution">
      {institution}
    </p>
  </div>
</footer>

<style>
  .footer {
    margin-top: 5rem;
    padding: 3rem 2rem;
    text-align: center;
    border-top: 1px solid rgba(176, 224, 168, 0.1);
  }

  .footer-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
    max-width: 90rem;
    margin: 0 auto;
  }

  .footer-text {
    color: var(--color-texto);
    font-size: 0.9rem;
    margin: 0;
  }

  .footer-divider {
    width: 2rem;
    height: 1px;
    background-color: var(--color-principal);
  }

  .footer-institution {
    color: var(--color-principal);
    font-size: 0.9rem;
    font-weight: 600;
    margin: 0;
  }

  @media (min-width: 768px) {
    .footer-content {
      flex-direction: row;
      justify-content: center;
    }
  }
</style>